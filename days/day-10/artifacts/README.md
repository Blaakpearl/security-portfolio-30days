# Day 10 — Artifacts Index

This folder contains evidence artifacts from the Day 10 lateral movement hunt lab.

| File | Description | Generated By |
|------|-------------|-------------|
| `baseline_auth_pattern.json` | 30-day authentication baseline for compromised account | PowerShell (Step 1) |
| `ntlm_type3_events.json` | All NTLM Type 3 logon events in hunt window | PowerShell (Step 2) |
| `pth_anomalies.json` | Pass-the-Hash anomalies flagged against baseline (if any) | PowerShell (Step 2) |
| `kerberos_tgt_events.json` | Kerberos TGT request events (Event 4768) | PowerShell (Step 3) |
| `kerberos_tgs_events.json` | Kerberos service ticket request events (Event 4769) | PowerShell (Step 3) |
| `wmi_dcom_execution.json` | Process creation events spawned by WmiPrvSE.exe/mmc.exe | PowerShell (Step 4) |
| `bloodhound_analysis.json` | Structured BloodHound blast radius findings | bloodhound_findings_documenter.py (Step 5) |
| `bloodhound_summary.txt` | BloodHound analysis console output | bloodhound_findings_documenter.py (Step 5) |
| `lateral_movement_hunt_playbook.md` | Reusable 6-step hunt methodology for future incidents | Step 6 |

## Key Finding Summary

- **Findings 01-03:** Negative — no Pass-the-Hash, Pass-the-Ticket, or WMI/DCOM
  lateral movement detected during the 42.3-hour compromise window
- **Finding 04:** Critical — `svc_backup` (Backup Operators group) has a historical
  session on the compromised host. Timing confirmation against the compromise
  window is the top-priority follow-up action.

> Commit your real output files here after running the lab scripts.
> BloodHound collection requires an authorized lab AD environment — SharpHound
> should never be run against production infrastructure without explicit authorization.
